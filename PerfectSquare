class Solution {
public:
    bool isPerfectSquare( int num)
    {
        if(num==1) 
            return true;
       long int i=1;
       
        while(i*i<=num)
            i++;
        i--;
        if(i*i==num)
            return true;
        else
            return false;
        
    }
};


