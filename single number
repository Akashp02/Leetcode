class Solution {
public:
    int singleNumber(vector<int>& nums) 
    {
         int n = nums.size();
         int op = 0;
        for(int i=0;i<n;i++)
        {
            op ^= nums[i];
        }
        return op;
    }
};
